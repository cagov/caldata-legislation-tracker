# `transform/` → `silver`

Transform layer. Add Databricks SQL files here that clean, type, and validate the
`bronze.*` tables into `silver.*` tables (materialized views or streaming tables),
applying expectations for data quality.

Placeholder — no pipeline logic yet.
